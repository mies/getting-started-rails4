box: wercker/ubuntu12.04-ruby2.0.0
services:
  - wercker/postgresql9.2@0.0.1
build:
  steps:
    - bundle-install
    - rails-database-yml:
        service: postgresql9.2
    - script:
        name: Set up db
        code: RAILS_ENV=test bundle exec rake db:schema:load
